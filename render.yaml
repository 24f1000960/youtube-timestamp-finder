services:
  - type: web
    name: youtube-timestamp-finder
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: AIPIPE_TOKEN
        sync: false   # you'll enter this manually in Render dashboard